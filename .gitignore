# Binaries
/pi-controller
/pic
/bin/

# Go build artifacts
*.test
*.out
coverage.txt

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local config + state (these belong under ~/.pi/run, not in repo)
/run/

# TypeScript / bun
/attach/node_modules/
/attach/*.bun-build
/attach/.*.bun-build

# Override global ignore — tasks/ holds the spec, plan, and todo for this project
/tasks/
/pi-controller
/pic
